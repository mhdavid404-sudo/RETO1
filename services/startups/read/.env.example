# read-startup-service no verifica JWT (endpoint publico), por eso no
# necesita JWT_SECRET — solo las variables de conexion a la DB.
DB_HOST=db
DB_PORT=5432
DB_NAME=reto1
DB_USER=reto1
DB_PASSWORD=changeme
