# read-startup-service no verifica JWT (endpoint publico), por eso no
# necesita JWT_SECRET — solo las variables de conexion a la DB.
DB_HOST=db
DB_PORT=5432
DB_NAME=reto1
DB_USER=reto1
DB_PASSWORD=changeme

# CORS restringido a este origen (shared/cors.py) — si lo usa, aunque
# no verifique JWT.
FRONTEND_ORIGIN=http://localhost:5173
