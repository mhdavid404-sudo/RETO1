DB_HOST=db
DB_PORT=5432
DB_NAME=reto1
DB_USER=reto1
DB_PASSWORD=changeme

# Debe ser identico al JWT_SECRET de auth-service.
JWT_SECRET=changeme

# CORS restringido a este origen (shared/cors.py).
FRONTEND_ORIGIN=http://localhost:5173
