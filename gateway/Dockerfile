# gateway/Dockerfile — nginx puro, sin build de codigo propio (brief
# seccion 2: "API Gateway | Nginx (reverse proxy puro)").
FROM nginx:1.27-alpine

COPY gateway/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
